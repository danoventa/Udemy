let age = 30;
if(true){
    let age = 25;
    console.log(age);
};
console.log(age);

// Scope of variables, var wouldn't do this.

const agze = 90;
// agze = 90 would error out, age is now a constant.
console.log(agze);